paths:
  /api/carts:
    post:
      summary: Crea un nuevo carrito.
      tags:
        - Carrito
      responses:
        "200":
          description: Carrito creado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Carts'